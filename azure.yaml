# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: petstore-jboss-eap
services:
  web:
    project: ./src/jbossappser-construction/petstore-ee7
    language: "java"
    host: appservice